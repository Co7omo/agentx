---
description: Fix all linting errors in the project
allowed-tools: ["Bash", "Read", "Edit"]
---

Run `npm run lint` and fix all reported issues. For each error:

1. Read the file with the error
2. Understand the linting rule
3. Fix the issue
4. Re-run lint to verify

Do not disable lint rules unless absolutely necessary.
